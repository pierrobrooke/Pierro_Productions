---
category: Feedback
---

# VeinLoader

Loading indicator built on the Vein Knot mark — wing-vein lines draw toward the center and bloom Electric Orchid, on loop. The `label` is a tracked-uppercase status line. Use for loading and "thinking" states.

```tsx
<VeinLoader label="Calibrating" />
```

Static under `prefers-reduced-motion`.
